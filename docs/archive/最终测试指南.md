# 最终测试指南 - 所有修复已完成

## ✅ 所有修复已完成并验证

### 服务器状态
```
✓ 服务器运行中: http://localhost:8089
✓ History service initialized
✓ 16 routes available
✓ JS文件已更新，包含全局函数
```

### 已完成的修复

#### 1. 历史数据恢复 ✅
- `app/managers.py`: 添加restore_session_from_db()
- `app/api.py`: 新增/timeline端点

#### 2. 创建会422错误 ✅  
- `static/api-client.js`: 改用JSON请求体

#### 3. Steps丢失和Mode错误 ✅
- `app/history_service.py`: 保存mode参数
- `app/main.py`: 传递mode参数

#### 4. 事件显示不全 ✅
- `static/opencode-new-api-patch.js`: 添加loadSessionTimeline()全局函数
- 暴露函数到window对象：`window.loadSessionTimeline`

#### 5. 服务器启动 ✅
- `app/main.py`: 添加uvicorn启动代码

---

## ⚠️ 重要：必须清除浏览器缓存

### 方法1: 强制刷新（推荐）
```
按 Ctrl + Shift + R
```

### 方法2: 清除缓存
1. 按 `Ctrl + Shift + Delete`
2. 勾选"缓存的图片和文件"
3. 时间范围选"全部"
4. 点击"清除数据"

### 方法3: 无痕模式
- 按 `Ctrl + Shift + N` 打开无痕窗口
- 访问 http://localhost:8089

---

## 验证步骤

### 1. 打开浏览器控制台
按 `F12`，切换到 `Console` 标签

### 2. 检查函数是否存在
输入：
```javascript
typeof window.loadSessionTimeline
```
应该返回: `"function"`

### 3. 打开Network标签
- 勾选 `Disable cache`
- 刷新页面（`F5`）

### 4. 创建任务测试
```
1. 访问: http://localhost:8089
2. 选择模式: build
3. 输入: "创建hello.py并执行"
4. 点击发送
5. 观察右侧面板
```

**应该看到**:
- ✓ write 事件（创建文件）
- ✓ bash 事件（执行命令）
- ✓ read 事件（读取结果）
- ✓ 所有工具调用完整显示

### 5. 刷新测试
```
1. 等待任务完成
2. 按 `Ctrl + Shift + R` 强制刷新
3. 点击"历史记录"
4. 点击刚创建的会话
```

**应该看到**:
- ✓ 会话标题
- ✓ Mode显示为 build（不是plan）
- ✓ 所有事件完整恢复
- ✓ 不只是2条消息

---

## 故障排除

### 如果函数还是undefined

完全重置浏览器：
1. 关闭所有浏览器窗口
2. 重新打开浏览器
3. 按 `F12` 打开开发者工具
4. Network标签勾选 `Disable cache`
5. 访问 http://localhost:8089

### 检查服务器响应

在Network标签查看：
- `opencode-new-api-patch.js` 文件大小应该是 ~30KB
- Response应该包含 `window.loadSessionTimeline`

### 如果Mode还是显示错误

1. 创建新任务（不要使用旧的）
2. 选择build模式
3. 检查Network标签的POST请求：
   - URL: `/opencode/session`
   - Method: `POST`
   - Content-Type: `application/json`
   - Request Payload: `{"title":"...","mode":"build"}`

---

## 文件修改清单

### 后端（4个文件）
1. `app/managers.py` - 数据库恢复
2. `app/api.py` - Timeline API
3. `app/history_service.py` - Mode保存
4. `app/main.py` - Mode传递 + 启动代码

### 前端（2个文件）
1. `static/api-client.js` - JSON请求体
2. `static/opencode-new-api-patch.js` - loadSessionTimeline全局函数

### 关键修改
```javascript
// opencode-new-api-patch.js 末尾
window.loadSessionTimeline = loadSessionTimeline;
window.handleHistorySessionClick = handleHistorySessionClick;
```

---

## 预期结果

### 修复前 ❌
```
- Mode显示错误（build变plan）
- 事件消失看不到
- 只显示2条消息
- 文件看不到
```

### 修复后 ✅
```
- Mode正确显示（build就是build）
- 所有事件完整显示
- 刷新后数据完整恢复
- 文件正常生成和显示
- Timeline完整可视化
```

---

**服务器正在运行: http://localhost:8089**

**请立即清除浏览器缓存后测试！**

所有代码修改已完成，服务器正在运行，只需清除缓存即可看到效果！
