# 安全漏洞修复补丁

## server_manager.py 修复内容

### 1. 健康检查添加SSL验证配置 (P0)
- 添加了SSL验证配置，通过环境变量OPENCODE_SSL_VERIFY控制
- 增加了超时配置和更详细的错误处理
- 添加了URL格式验证

### 2. HTTP请求添加SSL验证配置 (P0)
- 在execute方法中添加了SSL验证配置
- 设置了更详细的超时参数
- 添加了User-Agent头部
- 改进了错误处理，区分不同类型的HTTP错误

### 3. 子进程创建时添加资源限制 (P0)
- 添加了进程组创建（Unix系统）
- 实现了资源限制（CPU时间、内存、文件描述符）
- 改进了进程终止逻辑，支持进程组终止

### 4. 改进进程停止方法
- 使用信号量优雅终止进程
- 添加了进程组终止支持
- 改进了错误处理和日志记录

## main.py 修复内容

### 1. CORS配置限制允许的来源 (P0)
- 移除了通配符"*"，改为从环境变量读取允许的来源
- 限制了允许的HTTP方法
- 保留了允许的头部

### 2. 路径遍历漏洞修复 (P0)
- 在get_file_content和read_file函数中添加了路径规范化
- 验证文件路径在工作空间内
- 添加了危险字符检查
- 实现了文件大小限制
- 添加了允许的文件扩展名白名单

### 3. 数据库事务处理 (P1)
- 使用事务确保数据一致性
- 添加了PRAGMA foreign_keys设置
- 改进了错误处理和资源清理
- 使用with语句确保连接关闭

### 4. process_log_line函数拆分 (P1)
- 拆分为多个专门的函数：
  - _handle_session_mapping
  - _handle_todowrite_event
  - _handle_tool_use_event
  - _save_tool_part_to_history
  - _handle_text_event
  - _handle_error_event
  - _handle_thought_parsing
  - _handle_fallback_text

### 5. 参数验证改进
- 添加了参数长度限制
- 检查特殊字符
- 验证session_id格式
- 添加了文件路径安全检查

### 6. 实现Early Exit原则
- 所有函数都添加了参数验证
- 在函数顶部处理错误情况
- 使用guard clauses减少嵌套

### 7. 使用更具体的异常类型
- 区分sqlite3.Error和其他异常
- 使用HTTPException替代通用异常
- 添加了ProcessLookupError处理

### 8. 添加适当的日志级别
- 使用debug记录详细信息
- 使用warning记录非严重错误
- 使用error记录严重错误
- 添加了exc_info=True记录堆栈跟踪

## 代码质量改进

1. **原子性操作**: 数据库操作使用事务确保原子性
2. **资源管理**: 使用with语句确保资源正确释放
3. **错误恢复**: 添加了连接错误检测和服务器重启机制
4. **参数验证**: 所有公共函数都添加了输入验证
5. **防御性编程**: 添加了多层安全检查

## 注意事项

1. 需要设置环境变量CORS_ORIGINS来配置允许的来源
2. 可通过OPENCODE_SSL_VERIFY环境变量控制SSL验证
3. 文件大小限制设置为10MB（二进制文件）和5MB（文本文件）
4. 数据库连接超时设置为30秒
5. 进程资源限制：CPU时间5分钟，内存1GB，文件描述符100个